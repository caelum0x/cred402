apiVersion: v1
kind: Service
metadata:
  name: {{ include "cred402.fullname" . }}
  labels:
    {{- include "cred402.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cred402.selectorLabels" . | nindent 4 }}
