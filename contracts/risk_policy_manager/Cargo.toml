[package]
name = "risk_policy_manager"
version = "0.1.0"
edition = "2021"

[dependencies]
odra = { workspace = true }

[dev-dependencies]
odra-test = { workspace = true }

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
